#ifndef READ_FILE_H
#define READ_FILE_H

#include <stdio.h>

#include "struct.h"

//int check_file_size(FILE *f_open);
int check_data(FILE *f_open, products_t *products);
//int check_data(FILE *f_open, products_t *products);

#endif